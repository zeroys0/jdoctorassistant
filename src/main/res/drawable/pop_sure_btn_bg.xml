<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 填充颜色 -->
    <solid android:color="@color/login_loginBtn_bgColor" />

    <!-- 4个角度的弧度设置 -->
    <corners
        android:bottomLeftRadius="@dimen/pop_sure_btn"
        android:bottomRightRadius="@dimen/pop_sure_btn"
        android:topLeftRadius="@dimen/pop_sure_btn"
        android:topRightRadius="@dimen/pop_sure_btn" />

</shape>